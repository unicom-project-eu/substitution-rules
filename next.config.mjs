/** @type {import('next').NextConfig} */
// const nextConfig = { reactStrictMode: true, };

const nextConfig = {
    output: 'export',
    basePath: '/testrepo',
    assetPrefix: '/testrepo/',
};


export default nextConfig;
